{
  "name": "edutoken-project",
  "version": "0.0.1",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "ethers": "^6.8.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1"
  }
}
